

class App:
    users = []
    courses = []
    labs = []

    def command(self, userstring):    # userString is what the user typed on the command line
        return ""               # the return value will be the applications response
    def login(self, userName, password):
        return ""
    def logout(self):
        return
    def done(self):
        return True;  # will let user know that they're done
